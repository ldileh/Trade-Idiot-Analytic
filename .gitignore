# Node / frontend
node_modules/
dist/
dist-ssr/
*.local
.vite/

# Tauri / Rust
src-tauri/target/
src-tauri/bin/
src-tauri/gen/

# Python backend
backend/.venv/
backend/venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
backend/build/
backend/dist/
*.spec.bak

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example

# Tauri updater signing PRIVATE key — never commit. The .pub is committed.
src-tauri/tauri-updater.key

# Claude Code — local-only settings (shared skills/ are committed)
.claude/settings.local.json

# graphify — generated knowledge graph; rebuild per-machine with /graphify-sync
graphify-out/
