# Copy to .env and fill in. .env is gitignored; this template is committed.
# Realtime US quotes via Finnhub (free key: https://finnhub.io/register).
# Leave empty to use yfinance only (~15 min delayed). IDX (.JK) always uses yfinance.
FINNHUB_API_KEY=
